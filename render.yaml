services:
  - type: web
    name: anecdotal
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_STRING
        sync: false  # Set this in Render dashboard as a secret
